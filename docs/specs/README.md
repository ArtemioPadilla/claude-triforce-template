# Feature Specifications

This directory contains feature specifications created by the PM agent (Prometeo).

Each spec follows a structured format with problem statement, user stories, 
acceptance criteria, business rules, and risk assessment.

## Naming Convention
`{feature-name}.md` — use kebab-case.
